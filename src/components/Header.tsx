import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ backgroundColor: '#eee', padding: '1rem', textAlign: 'center' }}>
      <h1>Welcome to the Router</h1>
    </header>
  );
};

export default Header;